# EventHub API base URL — include the API prefix exactly as the backend serves it
NUXT_PUBLIC_API_BASE=http://localhost:8000/

# Public URL of this frontend (used for canonical/OG URLs)
NUXT_PUBLIC_APP_URL=http://localhost:3000
